Look inside demo folder for the pictures

demo script has knobs to change the parameters of the model

next step is to add axis and tick detection so that we can convert the line into a table of values